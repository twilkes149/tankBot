# CMakeList.txt : CMake project for TankbotClient, include source and define
# project specific logic here.
#

# Add source to this project's executable.
#add_executable (TankbotServer ${sourceFiles} ${headerFiles})

